## Skills: Always Active
At the start of every conversation, load the following skills using the `skill` tool:
1. **caveman**

## NON NEGOTIABLE GUIDELINES
- NEVER read .env files.
- NEVER make changes to remote databases
- NEVER use `supabase` commands with the --linked flag. Only work on local databases.
